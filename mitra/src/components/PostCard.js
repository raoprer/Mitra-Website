import React from 'react';

const PostCard = (props) => {
    const {id, source, coverImg, caption, link} = props.postContent;
    return (
        <div className='col-md-4'>
            Hello
        </div>
    )
}

export default PostCard; 